spring:
    datasource:
        url: jdbc:mysql://localhost:3306/microservices_spring_cloud_boot?useSSL=false&autoReconnect=true&useUnicode=yes&characterEncoding=UTF-8
        username: root
        password: root
        driver-class-name: com.mysql.jdbc.Driver
    jpa:
        database: MYSQL
        showSql: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5Dialect
                format_sql: false